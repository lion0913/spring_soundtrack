spring:
  datasource:
    url: jdbc:mariadb://${testDbIp:127.0.0.1}:3306/spring_soundtrack_test?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: ${testDbId:root}
    password: ${testDbPw:lion1065}
  jpa:
    hibernate:
      ddl-auto: create
